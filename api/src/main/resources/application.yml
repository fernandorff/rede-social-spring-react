spring:
  datasource:
    url: jdbc:postgresql://containers-us-west-89.railway.app:5654/railway
    username: postgres
    password: 6SVV3jtqvFR8Q5Z0HMGw
  jpa:
    hibernate:
      ddl-auto: validate

server:
  error:
    include-message: always

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
